# Visit Counter App- backend
Built with:
- Python's Flask for backend. 
- Redis for key-value data storage.
- Docker for local development.
## Quick start:
On your terminal, run:
```bash
make run-local
```
to start the app locally. By default, it will be live on http://localhost:5000, with the redis server at `redis://localhost:6379/`